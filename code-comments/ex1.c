// BSD License
#include "LICENSE.h"

#include <stdio.h>

int main(void) {
    int x, y;
    printf("Enter x and y: ");
    scanf("%d %d", &x, &y);
    printf("%d + %d = %d\n", x, y, x + y);
    return 0;
}
